// Category colors used on map pins, list card dots, filter buttons, and detail badges.
// Change a color here and it updates everywhere across the app.
// Keys must match the category strings stored in the database.
export const CATEGORY_COLORS: Record<string, string> = {
  food: "#e07040",
  drink: "#7c4fc4",
  activity: "#2d9e4a",
  stays: "#0891b2",
};

// Color used when a place has no recognized category
export const CATEGORY_COLOR_DEFAULT = "#9ca3af";
